const assert = require('assert');
const Physicist = require('../models/physicist')

describe('Saving to database', () => {
	// TEST CODE HERE

	// it('create and save a record', (done) => {
	// 	var Person = new Physicist({
	// 		'name': 'Stephen Hawking'
	// 	});

	// 	Person.save(() => {
	// 		assert(Person.isNew === false);
	// 		done();
	// 	});
	// });


});