export namespace Models {
	export interface Skills {
		name: string;
		title: string;
	}

	export interface Navs {
		name: string;
	}

	export interface Photos {
		id: number;
		index: number;
		thumbnail: string;
		url: string;
		src: string;
	}
}